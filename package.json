{
  "name": "horariositam-express",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "immutability-helper": "^2.8.1",
    "moment": "^2.22.2",
    "pg-promise": "^8.5.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "cd web && npm install && npm run build"
  }
}
